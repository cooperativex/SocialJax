name: SocialJax
channels:
  - conda-forge
dependencies:
  - python=3.10

  # SocialJax deps (conda-forge)
  - flax==0.10.7
  - scipy==1.12.0
  - distrax==0.1.5
  - safetensors==0.5.2
  - hydra-core==1.3.2
  - wandb==0.18.5

  - pip
  - pip:
    # JAX 0.6.2 with CUDA 12 wheels — bundles CUDA via pip; no cudatoolkit needed.
    - "jax[cuda12]==0.6.2"
    - gymnax==0.0.8
